# TimFit CRM

Публичная страница: `/`
Админка: `/admin`

Переменные Vercel:
- `VITE_SUPABASE_URL`
- `VITE_SUPABASE_ANON_KEY`
- `VITE_ADMIN_PASSWORD` — пароль админки, если не задан, используется `timfit2026`

После изменения переменных сделайте Redeploy.
